# GitHub Actions 测试文件

这是一个用于测试 GitHub Actions 工作流的文件。

## 测试内容

- 验证 Maven 构建缓存是否正常工作
- 验证 Docker 构建缓存是否正常工作
- 检查构建和测试流程是否正常

## 期望结果

1. 普通构建任务应该使用 Maven 缓存（已配置）
2. Docker 构建任务应该使用新添加的缓存配置
3. 所有构建和测试应该成功完成

测试时间: $(date +"%Y-%m-%d %H:%M:%S")